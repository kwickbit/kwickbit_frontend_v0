import React, { ReactNode } from "react";

//Used when get api returns an error, replace with some error page
const ServerError = (): ReactNode => {
  return <div>ServerError</div>;
};

export default ServerError;
